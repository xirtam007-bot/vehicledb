services:
  - type: web
    name: vin-scanner-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: MONGO_URI
        sync: false
      - key: API_KEY
        sync: false 
